import 'package:flutter/material.dart';

class GrabbingBar extends StatelessWidget {
  const GrabbingBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container() ;
  }
}
